<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="right"
    android:layout_marginBottom="10dp"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tvLabel"
        android:layout_width="@dimen/product_parameter_label_width"
        android:layout_height="wrap_content"
        android:textSize="@dimen/product_detail_text_size" />

    <EditText
        android:id="@+id/etValue"
        android:layout_width="@dimen/product_detail_input_width"
        android:layout_height="@dimen/product_detail_input_height"
        android:inputType="numberDecimal"
        android:maxLength="5"
        android:singleLine="true"
        android:gravity="center"
        android:layout_gravity="top"
        android:textSize="@dimen/product_detail_text_size"
        android:imeOptions="flagNoExtractUi" />

    <TextView
        android:id="@+id/tvUnit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/product_parameter_unit_text_size"
        android:layout_marginLeft="10dp" />

</LinearLayout>